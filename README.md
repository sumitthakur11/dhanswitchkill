# 🚀 DhanSwitchkill

DhanSwitch is a Python-based automation script that requires secure environment configuration before execution. This guide walks you through setting up the environment and running the script correctly.

---

## 📌 Prerequisites

Make sure you have the following installed:

- **Python 3.10 or higher**
- **pip**
- **Git** (optional)

```bash
Check Python version:

python --version

🔐 Step 1: open .env file and Configure Environment Variables replace your token 
ACCESS_TOKEN=your_access_token_here
LOSS_THRESHOLD=your_loss_threshold_here

🐍 Step 2: Create Python Virtual Environment
python -m venv venv


▶️ Step 3: Activate Virtual Environment
Linux / macOS
source venv/bin/activate
Windows
venv\Scripts\activate
📂 Step 4: Navigate to Virtual Environment Directory
cd venv
cd dhanswitchkill
▶️ Step 6: Execute the Script
python executefreeze.py


